MEDIASTACK — SESSION HANDOFF
Date: 2026-08-22
Repo: github.com/Alethio36/mediastack   Git authority: Harkon1   Deploy/verify: anzac3   Prod: anzac2 (old stack, untouched)
Schema: SCRIPT_SCHEMA=12   Harness: /tmp/h4 (142 tests passing)
Sandbox HEAD (last commit): 2db0a61 docs(readme): wire scope row was missing lazylibrarian
Harkon1 = origin, confirmed congruent this session (clean tree, matching message sequence).


== WHAT WAS BEING WORKED ON ==
App-expansion wave: new media services + polish, all shipped/verified live on anzac3.
Delivery pipeline unchanged: sandbox commit -> format-patch + sha256 -> Nick verifies -> git am -> push -> anzac3 upgrade.


== DONE (shipped, wired, verified live on anzac3) ==
New services (each its own fragment, stack UID, traefik router, config on restore points):
  - watchstate  (feat1) play-state sync/backup, GUID-matched. ONE instance rule. router watch.
  - navidrome   (feat3) music server over lidarr's /data/media/music (read-only). router music.
  - audiobookshelf (feat4) audiobooks+podcasts. router audiobooks.
  - kavita      (feat4) ebooks/comics/manga. router books.
  - lazylibrarian (feat5) book acquisition (Readarr replacement), VPN namespace. router books-dl.
      * first-class Prowlarr app (indexers sync like arrs), qbit download client, /data/media/{books,audiobooks}
      * MANUAL first-run by design: open UI -> Config>Interface -> set login -> Save -> restart -> re-run wire
Features/fixes this session:
  - jellyfin Webhook plugin now installed by wire (feat2) — WatchState webhooks + hub both need it
  - jellyfin server name set by wire (was container-ID hash)
  - set-credentials: added pihole+traefik; 'all' = one password across whole stack (Wizarr admin excluded)
  - provisioner: media/ tree grew audiobooks/podcasts/comics/manga; torrent/ stays lean
  - upgrade closing line now path-aware (compose->up / script->nothing / docs->nothing)
  - kavita healthcheck: image ships neither wget nor nc — uses curl /api/health (fix15/16)
  - lazylibrarian readiness+healthcheck accept /->/ home 303 (fix17)
  - docs sweep + candidates.md watchlist

Serving now complete: video(jellyfin) music(navidrome) audiobooks/podcasts(audiobookshelf)
  ebooks/comics/manga(kavita). Acquisition: arrs + lazylibrarian, all VPN'd, Prowlarr-fed.

WatchState state on anzac3: 13 anzac2 household identities + main provisioned via Single Backend Mode
  (temporarily deleted anzac3 backend to expose the switch). anzac2 backend = Import on / Export off.
  Hourly import task live. Per-user prod-state backups banked. Beta jellyfin export pending real libraries.


== DEFERRED (parked in docs/candidates.md with reasoning + revisit triggers) ==
  - ROMM      (Nick chose defer) first DB-backed service (app+MariaDB+Valkey, IGDB key).
              3 options recorded: (a) full DB-aware backup, (b) contained self-dump [LEANING], (c) defer [chosen].
  - Kapowarr  scrapes GetComics, NOT Prowlarr-integrated, needs ComicVine key. Doesn't fit shared-indexer model.
  - Bindery   young, Usenet-first vs this torrent stack.
  - Komga     Kavita chosen for breadth; Komga's API edge only matters for scripting.


== NEXT STEPS (in priority order) ==
1. MIGRATION anzac2 -> anzac3 — BLOCKED on Nick's decision. All discovery banked:
     - old compose + inventory captured. UID map already matches. Arr root folders already /data/media/{tv,movies,music} (ZERO remap).
     - qbit needs /torrent -> /data/torrent remap (transitional bind + Set-Location UI pass) for mid-flight downloads.
     - jellyfin lsio->official DB relocation in docs/migration-existing.md; plugins ride config rsync (Webhook already installed on anzac2).
     - WatchState bridges watch state so new jellyfin uses clean /data/media/* paths (no path preservation needed).
     - DECISION PENDING: beta isolation. 5 options presented; recommendation = subtree beta now + time-sliced rehearsal, optional music-slice graduation.
     - Still need from Nick: `qm config <anzac2-vmid>` + beta-isolation pick.
2. PRE-GO-LIVE checklist: BACKUP_ROOT->NAS via add-mount; ACME_ENV=production + up (check subdomain collisions); TRAEFIK_DOMAIN final.
3. DISCORD CHANNEL SPLIT (Nick's GUI task): seerr native Discord agent -> household channel (posters/embeds);
     hub streams -> separate ops channel. Native agents & hub must feed DIFFERENT channels to avoid dupes.
4. LazyLibrarian book indexers: 1337x synced (general tracker, low book hit-rate). Add a book-specific
     indexer (e.g. MyAnonaMouse) in Prowlarr for real book coverage. Content task, not wire.


== OPEN QUESTIONS / KNOWN GAPS ==
  - LazyLibrarian NOT wired to apprise hub: it has NO Apprise notifier (the "Apprise library installed" log line
    is a bundled dep for other notifiers). Only Slack/Telegram/Pushover/Prowl/email/custom_notify. Left unwired
    (documented gap) rather than build a fragile script bridge. The one stack service without native hub support.
  - Version column cosmetic bug (low priority): kavita shows base-OS "24.04" not app version — c_version parser
    reads wrong label for that image.


== KEY FILE PATHS ==
  compose.d/{watchstate,navidrome,audiobookshelf,kavita,lazylibrarian}.yml — new fragments
  mediastack.sh — wire_lazylibrarian, jf_plugin_webhook, jf_server_name, sc_rotate_* (arr/qbit/jellyfin/pihole/traefik), svc_url
  docs/candidates.md — the deferred-services watchlist (ROMM/Kapowarr/Bindery/Komga)
  docs/migration-existing.md — adoption/migration runbook (lsio->official jellyfin, etc.)


== STANDING RULES (still active) ==
  - Verify upstream against the DEPLOYED image tag, not master.
  - Never head-truncate contract files. Every patch-script replace asserts anchor is unique.
  - Probe-everything-at-once before choosing a healthcheck tool (nc/wget/curl lesson — burned 2 patches assuming nc).
  - Permissive healthchecks for SPA/redirect-fronted apps (accept 2xx/3xx).
  - anzac3 is the truth oracle; mocks prove code/mock agreement only.
  - Nick's long pastes auto-convert to EMPTY attachments — ask for trimmed inline text.
  - Delivery: never chain rm after git am; double-apply guard via git log before apply blocks.
